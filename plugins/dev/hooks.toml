# Claude Code Hooks for Catalyst Dev Plugin
# Automatically tracks thoughts documents in workflow context

[[hooks]]
name = "Track Research Documents"
event = "PostToolUse"
description = "Automatically add research documents to workflow context when written"

[hooks.matcher]
tool_name = "Write"
file_paths = ["*thoughts/shared/research/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

[[hooks]]
name = "Track Plan Documents"
event = "PostToolUse"
description = "Automatically add plan documents to workflow context when written"

[hooks.matcher]
tool_name = "Write"
file_paths = ["*thoughts/shared/plans/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

[[hooks]]
name = "Track Handoff Documents"
event = "PostToolUse"
description = "Automatically add handoff documents to workflow context when written"

[hooks.matcher]
tool_name = "Write"
file_paths = ["*thoughts/shared/handoffs/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

[[hooks]]
name = "Track PR Descriptions"
event = "PostToolUse"
description = "Automatically add PR descriptions to workflow context when written"

[hooks.matcher]
tool_name = "Write"
file_paths = ["*thoughts/shared/prs/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

# Also track edits to thoughts files
[[hooks]]
name = "Track Research Edits"
event = "PostToolUse"
description = "Update workflow context when research documents are edited"

[hooks.matcher]
tool_name = "Edit"
file_paths = ["*thoughts/shared/research/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

[[hooks]]
name = "Track Plan Edits"
event = "PostToolUse"
description = "Update workflow context when plan documents are edited"

[hooks.matcher]
tool_name = "Edit"
file_paths = ["*thoughts/shared/plans/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

[[hooks]]
name = "Track Handoff Edits"
event = "PostToolUse"
description = "Update workflow context when handoff documents are edited"

[hooks.matcher]
tool_name = "Edit"
file_paths = ["*thoughts/shared/handoffs/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

[[hooks]]
name = "Track PR Description Edits"
event = "PostToolUse"
description = "Update workflow context when PR descriptions are edited"

[hooks.matcher]
tool_name = "Edit"
file_paths = ["*thoughts/shared/prs/*"]

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/update-workflow-context.sh"]
run_in_background = false

# Plan Mode Integration
# Sync plan mode output to thoughts system and inject structure guidance

[[hooks]]
name = "Sync Plan Mode to Thoughts"
event = "PermissionRequest"
description = "Capture plan mode content and write to thoughts/shared/plans/ with Catalyst frontmatter"

[hooks.matcher]
tool_name = "ExitPlanMode"

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/sync-plan-to-thoughts.sh"]
run_in_background = false

[[hooks]]
name = "Inject Plan Structure Guidance"
event = "UserPromptSubmit"
description = "Provide Catalyst plan structure guidance when in plan mode"

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/inject-plan-template.sh"]
run_in_background = false

# CTL-1397/CTL-1420: replica-first Linear reads. Warn on (observe, default) or block
# (enforce) a bare `linearis issues read <ID>` that bypasses the local replica and 429s
# the shared Linear quota, and emit a catalyst.replica.read_fallback event so the burn-down
# is measurable in Loki. Mode via CATALYST_LINEAR_READ_DETECT_MODE (observe|enforce). This
# is what makes the detector actually FIRE (no manual settings.json install).
[[hooks]]
name = "Detect bare Linear reads (replica-first)"
event = "PreToolUse"
description = "Observe/enforce bare `linearis issues read` that bypasses the replica (CTL-1397/CTL-1420)"

[hooks.matcher]
tool_name = "Bash"

[hooks.command]
command = "bash"
args = ["${CLAUDE_PLUGIN_ROOT}/hooks/detect-bare-linear-read.sh"]
run_in_background = false
